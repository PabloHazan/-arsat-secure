{
  "name": "@arsat/secure",
  "version": "1.0.3",
  "description": "Firmar y encriptar propiedades",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc",
    "pretest": "npm run clean && jest --clearCache",
    "test": "jest --coverage",
    "prepublish": "npm run build",
    "publish": "npm publish"
  },
  "keywords": [
    "secure",
    "performance",
    "singnature",
    "stateless"
  ],
  "author": "phazan",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.13.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.7.3"
  }
}